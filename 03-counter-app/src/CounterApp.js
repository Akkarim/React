import propTypes 

const CounterApp = ({value}) => {
    return (
        <>
        <h1>Counter App</h1>
        <h2>{ value }</h2>
        </>
    );
}

CounterApp.propTypes

export default CounterApp;
